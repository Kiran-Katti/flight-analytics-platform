1) Define scope - decide exactly what data you want to ingest and what business questions the dashboard will answer.

2) Decide on datasets
   -- Historical Flight Dataset (Primary)
	-- Airport Metadata Dataset (Reference)
	-- OpenSky API (Live Data Enrichment)

3) Create Github repo (flight-analytics-platform)

4) Create folder structure

flight-analytics-platform/

├── ingestion/
│
├── database/
│
├── dbt_project/
│
├── dashboard/
│
├── docs/
│
├── tests/
│
├── docker/
│
├── data/
│
├── .gitignore
│
├── README.md
│
└── docker-compose.yml

5) Create a Dockerized PostgreSQL Environment

6) Connect to database from dbms (Dbeaver)

7) Create a init.sql file in database folder

8) Commit changes to local git

9) Create required schemas

10) Download required datasets (ourairports.com, https://www.kaggle.com/datasets/giovamata/airlinedelaycauses?resource=download)

11) Profile the data

12) Create the required tables

13) Build ingestion pipeline (use batch/chunk loading whenever necessary)

14) Install dbt related dependencies

15) Create a dbt project

16) Create transformation files (Staging layer and analytics layer)
 
17) Create dbt tests and dbt documentation

18) 